# 導入手順

## 1. Amazonアソシエイト
- https://affiliate.amazon.co.jp/ で登録（Xアカウントをサイトとして登録可能）
- アソシエイト・セントラル右上の「トラッキングID」を控える（例: `hangtime-22`）
- 注意: 登録後180日以内に3件の売上がないとアカウントが閉鎖される
- 短縮リンク（amzn.to）を使いたい場合はサイトストライプで作成し、`商品リスト.csv` の「短縮URL」列に貼る

## 2. X Developer Portal
- https://developer.x.com/ でアプリ作成（Free プランで投稿可能。月1,500投稿まで）
- 「User authentication settings」→ App permissions を **Read and write** に設定
- 「Keys and tokens」で以下を取得
  - API Key / API Key Secret
  - Access Token / Access Token Secret（権限変更後は **必ず再生成**）

## 3. Anthropic（Claude）
- https://console.anthropic.com/ で API キー発行
- 使用モデルの既定は `claude-opus-5`（`CLAUDE_MODEL` で変更可）

## 4. 商品リストの作成（動画の「4つの変数」）
`商品リスト.csv` を編集（UTF-8）。1行＝1商品で、動画の「変数」をそのまま列にしている

| 列 | 内容 | 入力例 |
|---|---|---|
| ASIN | 商品ページURLの `/dp/` 直後の英数字10桁 | `B0C5XXXXXX` |
| 商品名 | Amazon商品ページからコピーした名称 | `くもん出版 はじめてのひらがな 1集` |
| ジャンル | 発信するカテゴリ | `子供の教育・知育` |
| ターゲット | どんな悩みを持つ人か。本音に近い悩みも添えるほど精度が上がる | `3〜5歳の子を持つ母親。教え方が分からず、周りの子より遅れていないか不安` |
| 口調 | 投稿の雰囲気 | `ママ友系（親しみやすい丁寧語）` / `丁寧系` / `ため口（先輩ママ）` |
| 特徴 | 任意。「／」区切り。Claude が文面のネタにする | `1日1枚で続けやすい／シール付き` |
| 短縮URL | 任意。amzn.to を貼れば標準リンクの代わりに使用 | `https://amzn.to/xxxx` |

- サンプル行の ASIN（`B0XXXXXXX1` など）はダミー。必ず実際の ASIN に置き換える
- 指示文そのものを調整したい場合は `プロンプト.md` を編集（`{ジャンル}` などの差し込み記号は残す）
- `プロンプト.md` の `{…}` を手で埋めて Claude のチャットに貼れば、動画と同じ手動運用もできる

## 5. ローカルで動作確認
```bash
pip install -r requirements.txt
cp 環境変数の設定例.txt .env
# .env を編集
python 自動投稿.py --dry-run
```
- 候補3件と採点（伸びる確率・内訳・理由）、採用する1本目・2本目の文面が表示される
- 文面に問題なければ `python 自動投稿.py` で1件投稿し、X で「本文 → リプライにリンク」の形になっているか確認

## 6. GitHub Actions で自動化
- リポジトリ → Settings → Secrets and variables → Actions → **Secrets** に登録
  - `ANTHROPIC_API_KEY`
  - `X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_TOKEN_SECRET`
  - `AMAZON_ASSOCIATE_TAG`
- 同画面の **Variables**（任意）
  - `CLAUDE_MODEL`（既定 claude-opus-5）
  - `REPOST_INTERVAL_DAYS`（同一商品の再投稿間隔、既定 7）
- Settings → Actions → General → Workflow permissions を **Read and write** に設定（履歴コミット用）
- Actions タブ → 「Amazonアフィリエイト X自動投稿」→ Run workflow で `dry_run=true` を実行し動作確認
- 以降、毎日 12:00 / 20:00 JST に自動投稿

## 7. 投稿頻度の変更
`.github/workflows/自動投稿.yml` の `cron` を編集（UTC表記。JST − 9時間）

| JST | cron |
|---|---|
| 07:00 | `0 22 * * *` |
| 12:00 | `0 3 * * *` |
| 20:00 | `0 11 * * *` |

## 運用上の注意
- 1本目の末尾に `#PR`、リンクを貼る2本目のリンク直上に `【PR】` を必ず付ける（景品表示法のステマ規制対応。自動付与済み）
- リンクを本文ではなくリプライに置くのは、動画の Threads 運用と同じ構成。X でもリンク入り投稿は表示が伸びにくいため同じ形にしている
- Amazonアソシエイト規約により、価格の明記は避ける（価格帯表現に留める）
- 全商品が再投稿間隔内に投稿済みの場合はエラー終了する → 商品を追加するか間隔を短縮
- Claude が生成拒否した場合はサーバー側で別モデルへ自動フォールバックし、それでも拒否ならエラー終了
